services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: kingscup
      POSTGRES_PASSWORD: kingscup_dev
      POSTGRES_DB: kingscup
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
